; @polestar packages come from the nextgen_platform feed (no credentials here: use the
; Azure Artifacts Credential Provider locally and npmAuthenticate@0 in CI).
@polestar:registry=https://pkgs.dev.azure.com/polestar-products/nextgen_platform/_packaging/microservices-shared/npm/registry/
