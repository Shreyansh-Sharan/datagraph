# The backend suite. Run it from the repository root: `pytest`, or `make test`.
# pythonpath puts the package and the suite's own root on sys.path, so the tests import
# `ontoforge` and `tests.hr_fixture` whether or not the package is installed.
[pytest]
testpaths = backend/tests
pythonpath = backend/src backend
addopts = -q
